Let $G = (V,E)$ be a simple, undirected graph. Then we say that $G$ is complete as a graph, if for every pair of vertices $(v_1,v_2) \in V \times V$, there exists an edge $e_{v_1,v_2} \in E$ between the vertices $v_1,v_2 \in V$. 