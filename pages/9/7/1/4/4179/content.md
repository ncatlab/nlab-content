In the setting of [[partial function]]s and operations, saying that an equality between [[term|terms]] such as 
$$ s = t $$
is a **Kleene equality** means that it asserts that if either side of the equality is defined, then so is the other, and they are equal.
