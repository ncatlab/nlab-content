Let $(X,\rightarrow)$ be an abstract rewriting system ie. a set equipped with a binary relation $\rightarrow$. The Newman lemman asserts that:

\begin{proposition}
If $\rightarrow$ is [[strongly normalizing]] and [[locally confluent]] then, it is [[confluent]].
\end{proposition}